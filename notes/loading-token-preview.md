# Loading and token-page preview findings

At 390px, the public home keeps the crown header mark, gold CTA, centered hero, and sticky navigation balanced with no overlap. The design-token page remains readable on mobile: the crown header, material-language copy, chips, and glass panels stack cleanly within the viewport. The token page is intentionally compact, and the next panels continue below the fold without clipping the visible cards.
