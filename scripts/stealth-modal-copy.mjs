import fs from "node:fs";
const path = "/home/ubuntu/fortrex-fx/client/src/pages/Home.tsx";
let source = fs.readFileSync(path, "utf8");
const newPass = `{state === "pass" && <div className="space-y-5"><div className="relative overflow-hidden rounded-2xl border border-[#f2d18a]/55 bg-gradient-to-br from-[#4f3a18] via-[#1c1710] to-[#0b0d12] p-8 text-center shadow-[0_0_42px_rgba(201,151,62,.16)]"><div className="absolute right-6 top-6 opacity-80"><GoldMark /></div><div className="mb-8 text-[10px] font-bold uppercase tracking-[.24em] text-[#f2d18a]">FORTREX FX / PRIVATE ACCESS</div><div className="display text-4xl font-bold text-[#fff7e6]">You’re on the list.</div><p className="mx-auto mt-4 max-w-sm text-sm leading-7 text-[#d8cba9]">The first message will find you when Fortrex is ready to be seen.</p><div className="mt-8 flex items-center justify-center gap-2 text-xs uppercase tracking-[.18em] text-[#f2d18a]"><Check size={15} /> Early access reserved</div></div><div className="rounded-xl border border-[#f2d18a]/14 bg-white/[.025] p-5 text-center"><div className="mb-2 flex items-center justify-center gap-2 text-sm font-semibold text-[#fff7e6]"><Radio size={15} className="text-[#c9973e]" /> Keep the signal close</div><p className="text-sm leading-6 text-[#a99b7a]">Watch your inbox. The next reveal will not be public for long.</p></div><button onClick={onClose} className="primary-cta w-full rounded-full px-6 py-4 text-sm font-bold uppercase tracking-[.12em]">Return to the signal</button></div>}`;
source = source.replace(/\{state === "pass" && <div className="space-y-5">[\s\S]*?<\/div>\}\n    <\/div>/, `${newPass}\n    </div>`);
source = source.replace(/<p>Fortrex FX is a trading tournament and community platform\.[\s\S]*?<\/p><p>REX is a platform-only currency\.[\s\S]*?<\/p><p>Participation involves market risk\.[\s\S]*?<\/p>/, `<p>Fortrex FX is preparing a private product experience for traders who recognize meaningful signals early. This page is a pre-launch registration point, not a complete description of the product.</p><p>The details behind the Fortrex experience will be shared in stages with people on the private launch list. Registration does not represent a promise of access to every future feature or outcome.</p><p>Review the final terms, eligibility requirements, and launch communications when they are published before taking action.</p>`);
source = source.replace(/<p>We collect the information needed to manage Genesis registration[\s\S]*?<\/p><p>We do not ask for broker passwords[\s\S]*?<\/p><p>Contact, retention, deletion, security, and third-party processing details should be finalized with counsel and updated here before the public launch\.<\/p>/, `<p>We collect the information needed to place you on the private launch list, send launch communications, and understand interest in Fortrex.</p><p>Only submit information you are comfortable sharing. The final privacy notice will explain retention, deletion, security, and third-party processing before public launch.</p><p>We will update this page as the Fortrex experience moves from private signal to public release.</p>`);
fs.writeFileSync(path, source);
